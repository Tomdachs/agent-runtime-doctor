## Summary

## Validation

- [ ] `uv run ruff check .`
- [ ] `uv run ruff format --check .`
- [ ] `uv run pytest`

## Safety / schema

- [ ] Default behavior remains read-only.
- [ ] No secret values or raw environment values are added to reports.
- [ ] Schema/docs/tests are updated if public output changed.
